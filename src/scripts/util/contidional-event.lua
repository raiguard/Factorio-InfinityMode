-- ----------------------------------------------------------------------------------------------------
-- CONDITIONAL EVENT HANDLER
-- This script creates and handles conditional events.

local events_table_demo = {
    {defines.events.on_built_entity, defs.cheats.personal.instant_blueprint}
}

local conditional_event = {}

function conditional_event.register(event, handler)

end

return lib